const LoadingSpinner = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        height: "54.5vh",
        alignItems: "center",
      }}
    >
      <span className="loader"></span>
    </div>
  );
};

export default LoadingSpinner;
